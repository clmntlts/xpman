"""Non-Python assets (theme.qss, icons/) exposed as package data -- see xpman.gui.theme and
xpman.gui.icons, which load these via importlib.resources rather than a __file__-relative path
so they resolve correctly both in a normal checkout and inside a PyInstaller-frozen bundle."""
